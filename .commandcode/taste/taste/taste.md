# Taste
- Communicates in Indonesian (Bahasa Indonesia); prefers responses in Indonesian and Indonesian-language text in their applications' UI. Confidence: 0.9
- Wants the agent to fully explore and verify the codebase's actual state first (code, model files/artifacts, git state) before editing anything, so changes match reality rather than assumptions or stale docs. Confidence: 0.9
- Keeps some docs manually curated as source of truth (e.g., ARCHITECTURE.md, DATASET.md, DECISIONS.md) and expects the agent to respect those as off-limits, while proactively fixing any other .md files whose content no longer matches the code. Confidence: 0.8
- Expects housekeeping files like .gitignore to be updated in sync whenever docs/codebase alignment work is done. Confidence: 0.7
- Prefers runtime-tunable configuration (e.g., model path, inference parameters) to be exposed via environment variables with sensible defaults rather than hardcoded, so behavior can be adjusted for testing without editing code. Confidence: 0.7
- Wants changes verified by actually running the system — starting the backend and hitting endpoints, running the web app, and testing end-to-end flows with real images — not only static checks like lint/type-check/build. Confidence: 0.75
- Prefers a multi-page web app layout (e.g., separate Home and Inspect pages) over a single monolithic page. Confidence: 0.6
- Wants a durable project context file (AGENTS.md at repo root, auto-loaded each session) so agents have full project context without having to re-read the entire codebase on every new session. Confidence: 0.9
- Wants that context file to contain instructions telling agents to read only the specific relevant .md docs on demand (reference/import style, e.g., @docs/...), rather than scanning the whole code base, to save tokens and time. Confidence: 0.85
- For this product, the repo-root DESIGN.md is a live, authoritative design spec whose visual direction the user revises between sessions — it has already moved Linear/Vercel gray → Anthropic cream/coral (#faf9f5/#cc785c, serif display) → Apple-style (Action Blue #0066cc, white canvas + parchment #f5f5f7, near-black tiles, SF Pro/system font stack, pill CTAs, parchment footer). When asked to restyle ("sesuaikan lagi ya", "kurang cocok deh"), always re-read the current DESIGN.md and re-skin the whole UI (all pages/components, including the image viewer) to its current tokens — never reuse the style from a past session. Confidence: 0.9
- When asked for image zooming in a detection viewer, expects a full pro image-viewer UX — zoom in/out buttons with percentage readout, reset to 100%, fit-to-screen, wheel-zoom toward cursor, and drag-to-pan — and expects detection annotations (bbox borders and labels) to be rendered at constant screen size so they stay sharp/readable at any zoom level rather than shrinking with the image. Confidence: 0.75
- When asked to do work in a specialized domain (e.g., UI/UX design), expects the agent to activate and follow the relevant installed skills (e.g., ui-ux-pro-max, anti-ui-slop) rather than improvising generic output. Confidence: 0.85
- Has zero tolerance for generic/"AI slop" UI — explicitly rejected it twice across redesigns ("ini masih sangat terlihat ai slop, saya tidak suka", "masih jelek sekali") — and expects deliberate, professional UI/UX design with a coherent design system (typography, colors, spacing, complete interaction/error/empty states) instead of out-of-the-box or template styling. Confidence: 0.9
- Treats model evaluation metrics (per-class recall, mAP ~0.8+) as the bar production should meet; when live results miss defects or show low confidence despite good training scores, expects root-cause analysis reconciling eval-vs-inference behavior (post-processing thresholds, small-object downscaling) rather than acceptance. Confidence: 0.75
- For detection thresholds in production, prefers a balanced calibration: sensitive for recall-critical classes, raising thresholds only for classes proven to emit consistent low-confidence false positives — not blanket-extreme sensitivity, and not aggressive filters that silently disable a whole class. Confidence: 0.6
- When improving a trained model's live accuracy, prefers staged, low-effort wins first — inference-side optimizations (resolution/imgsz tuning, multi-resolution ensemble) proven by real experiments — before committing to a retrain; retraining is treated as a separate later step. Confidence: 0.6
- For the defect-inspection service, accepts meaningfully slower inference (e.g., ~2× for a multi-resolution ensemble on CPU) in exchange for higher, more consistent detection confidence/recall — detection quality outweighs latency. Confidence: 0.55
- Interactive UI (buttons, zoom, drag) must be verified with realistic pointer events (real Playwright clicks / mouse events), not synthetic DOM `.click()`/`page.evaluate` calls — synthetic events bypass browser handling like pointer capture and can make broken controls look fine until the user tries them with a real mouse. Pointer/viewer interaction bugs keep surfacing in real use (user reported zoom toolbar buttons dead while synthetic-click tests passed; later reported a plain click on the image unexpectedly jumping to 100% zoom because `pointerdown` activated manual zoom mode). Treat viewer pointer handling (click vs. drag thresholds, when user-controlled zoom engages) as high-risk code that needs browser-level verification. Confidence: 0.75
- Is cost-conscious about token usage: when explicitly asked, will accept a lighter verification plan (e.g., lint/typecheck/build plus curl-level HTTP checks of routes and auth/history flows) instead of full Playwright/browser visual testing, so the plan's verification section should offer and honor that trade-off. Confidence: 0.75
- Actively solicits the agent's suggestions for making the product more attractive and useful ("apakah kamu ada saran? agar webnya jadi lebih menarik dan bermanfaat? membosankan sekali kalau hanya deteksi biasa seperti itu"; later again "kira kira fitur apa lagi yang bisa ditambahkan untuk aplikasi ini?") and, when offered concrete recommended options (batch upload/export/filter, stats dashboard, etc.), chooses from them rather than insisting on a literal-minimum scope — so on feature requests the agent should propose reasoned, concrete ideas and present them as options, not just implement the bare ask. Confidence: 0.75
- When the user grants a green light with a short decisive instruction ("eksekusi saja semuanya"), they mean the FULL proposed scope — every listed feature — and they expect the agent to proceed autonomously and end-to-end (breaking work into a todo list, driving backend/schema/frontend changes, then verifying) without pausing again for item-by-item confirmation or heeding the agent's own prioritization suggestions. Confidence: 0.7
- For this product's web app, settled architecture for user accounts and inspection history is: Better Auth (email/password) on the Next.js server + server-side SQLite via Drizzle/better-sqlite3, with per-user history capped at the most recent 10 inspections and session-protected pages/APIs (unauthenticated access redirects/401s); expects a login/register flow and a History page with filter/search and per-item delete. Confidence: 0.8
- Holds the agent accountable for features it mentions or proposes: if a capability was raised as an option or implied as available (e.g., "pilih model untuk membandingkan"), the user expects it to actually exist in the UI and will file it as a complaint if missing ("kata kamu bisa memilih model kok ini tidak bisa?"). Don't advertise unbuilt features; when proposing options, mark what exists vs. what would be new. Confidence: 0.75
- Wants persisted inspection results (history cards/gallery) to display the actual annotated detection image — bounding boxes and class labels drawn on the stored thumbnail — not bare thumbnails or text-only lists; annotated visuals in the history are explicitly considered the attractive/expected state ("padahal itu cukup menarik kalau ada"). Confidence: 0.75
- Wants multi-model comparison supported in this product: model files kept in `backend/models/*.pt` with self-describing names (e.g., `best-e03v2-yolov8s.pt`), auto-discovered at startup, exposed via an API list, and selectable from a dropdown in the Inspect UI before running analysis. Confidence: 0.7
- Treats UI affordances/labels that misrepresent platform capabilities as defects: a "Kamera" capture button opening a file picker on desktop is legitimate browser behavior, but the interface must communicate that device difference honestly (e.g., a caption/tooltip) instead of leaving users to think it's broken. Confidence: 0.6
- For the inspect-page confidence threshold control, settled design: keep the free numeric slider and add three semantic preset buttons (Balanced / High precision / High recall) that set the slider; presets are global per model (one threshold for all classes), not per-class thresholds. Confidence: 0.7
- Wants threshold/preset values grounded in empirical calibration rather than arbitrary round numbers: when proposing values like balance 0.45 / high recall 0.30 / high precision 0.60, asks the agent to verify or decide from threshold-calibration experiments on the test set (precision/recall/F1 vs confidence per model). Aggregate artifacts alone (e.g., experiment_summary.json with mAP/precision/recall measured at very low conf) are considered insufficient — the agent should request per-detection eval data (predictions.json/CSV with is_tp/is_fp) or run a one-pass calibration script rather than guess. Confidence: 0.75
